# PA-GRPO direct dependencies.
#
# Versions below match the environment used in the paper. They list lower
# bounds; pip will resolve transitive deps automatically. If you need a
# bit-for-bit reproduction of our environment (e.g. a transitive package
# was yanked or has a backwards-incompatible patch), use the pinned
# `requirements-lock.txt` instead.
#
# Note: verl is NOT installed via pip — this repository ships a modified
# verl tree under ./verl/ (forked from upstream commit 0eb50ec4, v0.7.0.dev).
# Run training scripts from the project root so Python imports the local
# modified verl package.

# Core RL / RLHF stack
torch==2.6.0
torchaudio==2.6.0
torchvision==0.21.0
torchdata==0.11.0
vllm==0.8.5
ray[default]>=2.49.0
flash_attn==2.6.3
tensordict>=0.10.0,<0.11.0

# Model / tokenizer
transformers>=4.57.0
tokenizers>=0.22.0
peft>=0.17.0
safetensors>=0.6.0
sentencepiece>=0.2.0
huggingface-hub>=0.36.0
accelerate>=1.11.0

# Training utilities
liger_kernel>=0.6.3
hydra-core>=1.3.2
omegaconf>=2.3.0
pyarrow>=19.0.0
pandas>=2.3.0
datasets>=4.4.0
numpy<2.0.0
dill>=0.4.0
codetiming>=1.4.0
pylatexenc>=2.10
latex2sympy2_extended>=1.10.0
math-verify>=0.8.0

# Logging
tensorboard>=2.20.0
wandb>=0.22.0

# Service / API (used by verl rollout server)
fastapi>=0.121.0
uvicorn>=0.38.0

# Build / dev
packaging>=20.0
pybind11>=3.0.0
pre-commit>=4.4.0
